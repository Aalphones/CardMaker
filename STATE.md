# STATE

**Aktiver Plan:** `docs/planning/2026-08-01_fundament-und-grundgeruest/`
**Phase:** 4/9 — Login & Zugriffstoken im Backend (gebaut, Prüfung am Server offen)
**Nächster Schritt:** Backend hochladen (`deploy.cmd backend`), dann das erste und einzige
Konto über `POST /api/setup` anlegen und die vier Handproben aus `phase-4-auth-backend.md`
fahren. Der Code ist vollständig und lokal geprüft (Syntax + Proben ohne Datenbank); jede
SQL-Zeile ist noch ungetestet, weil es lokal keine Datenbank gibt. Erst danach ist Phase 4
`complete` und Phase 5 (Frontend-Gerüst, standard) dran.
